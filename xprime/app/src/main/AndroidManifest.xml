<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="XPrime Extension"
        android:supportsRtl="true">
        
        <meta-data
            android:name="provider_name"
            android:value="XPrime" />
        
        <meta-data
            android:name="provider_lang"
            android:value="en" />
        
        <meta-data
            android:name="provider_version"
            android:value="1.0.0" />
        
        <meta-data
            android:name="provider_class"
            android:value="com.shawken.xprime.XprimeProvider" />
            
    </application>
</manifest>